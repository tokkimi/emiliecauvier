# Base de données Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/emilie?schema=public"

# Auth.js — générer avec: openssl rand -base64 32
AUTH_SECRET="replace-me"

# Stripe
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_SUBSCRIPTION=""   # price_... abonnement mensuel 19$
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stockage des PDF (privé) — ex. S3/R2. Laisser vide en dev (sert /pdf local).
PDF_BUCKET_URL=""

# Compte admin créé par le seed (npm run seed)
ADMIN_EMAIL="emilie@labibliotheque.ca"
ADMIN_PASSWORD="ChangeMoi2026!"
